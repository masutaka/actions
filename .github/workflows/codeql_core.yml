# Derived from https://github.com/route06/actions/blob/main/.github/workflows/codeql_core.yml
# Copyright (c) 2024 ROUTE06, Inc.
# Licensed under the MIT License.

name: CodeQL Core

on:
  workflow_call:
    inputs:
      language:
        required: true
        type: string

jobs:
  analyze:
    name: Perform CodeQL for ${{ inputs.language }}
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@5d4e8d1aca955e8d8589aabd499c5cae939e33c7 # v4.31.9
        with:
          languages: ${{ inputs.language }}

      # コンパイルされた言語を解析するために必要
      # コンパイルされていない言語の場合は直ちに正常終了する
      - name: Auto build
        uses: github/codeql-action/autobuild@5d4e8d1aca955e8d8589aabd499c5cae939e33c7 # v4.31.9

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@5d4e8d1aca955e8d8589aabd499c5cae939e33c7 # v4.31.9
